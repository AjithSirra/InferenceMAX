name: Test Runner

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: h100-local
    steps:
      - name: Test runner
        run: |
          echo "Runner is working!"
          hostname
          docker --version
          nvidia-smi
