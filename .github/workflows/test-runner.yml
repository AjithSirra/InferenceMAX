name: Test Runner

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: h100-local-0
    steps:
      - name: Test runner
        run: |
          echo "Runner is working!"
          hostname
          docker --version
          nvidia-smi
